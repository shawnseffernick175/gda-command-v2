# F-023a Execution — Drop 5 Deferred Tables + ADOPT gda_touchpoints

**Date:** 2026-05-22
**Author:** Devin (automated execution)
**Status:** EXECUTED
**Issue:** [#258](https://github.com/shawnseffernick175/gda-command-v2/issues/258)
**Prerequisite:** F-023a diligence (PR #285), F-023 Step 2 (PR #284)

---

## Summary

| Action | Count | Details |
|--------|-------|---------|
| Tables dropped | 5 | All orphan data, 0 workflow/code refs, architect approved |
| Tables adopted | 1 | `gda_touchpoints` promoted to ADOPT, migration 058 generated |
| Migrations generated | 2 | 057 (`gda_relationships`) + 058 (`gda_touchpoints`) |
| n8n DB table count | 161 → 156 | Δ = -5 |
| Shadow table count | 69 → 64 | 17 dropped total (12 Step 2 + 5 F-023a) |
| ADOPT count | 27 → 28 | +1 (`gda_touchpoints`) |
| DEFERRED count | 6 → 0 | All resolved |

---

## Pre-State Snapshot

### 5 DROP Tables

| # | Table | Rows | Workflow Refs | Backend Refs | Inbound FKs | Archive |
|---|-------|------|-------------|-------------|------------|---------|
| 1 | `gda_incumbent_log` | 19 | 0 | 0 | 0 | `/tmp/f023-deferred-archive/gda_incumbent_log.sql` (6,499 bytes) |
| 2 | `gda_market_benchmarks` | 20 | 0 | 0 | 0 | `/tmp/f023-deferred-archive/gda_market_benchmarks.sql` (7,098 bytes) |
| 3 | `gda_pre_sam_intel` | 53 | 0 | 0 | 0 | `/tmp/f023-deferred-archive/gda_pre_sam_intel.sql` (52,415 bytes) |
| 4 | `gda_target_agencies` | 5 | 0 | 0 | 0 | `/tmp/f023-deferred-archive/gda_target_agencies.sql` (3,360 bytes) |
| 5 | `gda_vehicle_tracker` | 14 | 0 | 0 | 0 | `/tmp/f023-deferred-archive/gda_vehicle_tracker.sql` (4,241 bytes) |

Row counts re-confirmed via `COUNT(*)` — all match F-023a diligence report exactly.
Workflow refs re-confirmed via fresh n8n API export (158 workflows, active + inactive).
Backend/frontend/shared code re-confirmed via grep — 0 references.

### ADOPT Table: gda_touchpoints

| Field | Value |
|-------|-------|
| Rows | 0 |
| Columns | 7: `id` (serial PK), `relationship_id` (int FK), `contact_type` (text), `summary` (text), `next_action` (text), `next_action_date` (date), `created_at` (timestamptz) |
| FK | `relationship_id → gda_relationships(id)` |
| Index | `gda_touchpoints_pkey` (btree on `id`) |
| Workflow | `GDA.api.relationship-tracker` (ck1NTtdvuqB7CQ81) — active, never executed |

### ADOPT Table: gda_relationships (parent, migration dependency)

| Field | Value |
|-------|-------|
| Rows | 0 |
| Columns | 13: `id` (serial PK), `contact_name` (varchar), `title` (varchar), `agency` (varchar), `department` (varchar), `email` (varchar, PII), `phone` (varchar, PII), `relationship_strength` (varchar, default 'New'), `last_contact_date` (date), `notes` (text), `tags` (text[]), `created_at` (timestamptz), `updated_at` (timestamptz) |
| FK | None (is a parent table) |
| Index | `gda_relationships_pkey` (btree on `id`) |

**Schema divergence note:** The `GDA.api.relationship-tracker` workflow's `CREATE TABLE IF NOT EXISTS`
defines a different schema (e.g., `name TEXT NOT NULL` vs live `contact_name VARCHAR`, `relationship_strength
INTEGER CHECK(1..10)` vs live `VARCHAR DEFAULT 'New'`). The migration matches the **live schema**, not the
workflow's intended schema. The workflow's `CREATE TABLE IF NOT EXISTS` is a no-op since the table already exists.

---

## Drop Execution

| # | Table | Pre-Drop Rows | SQL | Response |
|---|-------|-------------|-----|---------|
| 1 | `gda_incumbent_log` | 19 | `DROP TABLE IF EXISTS gda_incumbent_log;` | DROP TABLE |
| 2 | `gda_market_benchmarks` | 20 | `DROP TABLE IF EXISTS gda_market_benchmarks;` | DROP TABLE |
| 3 | `gda_pre_sam_intel` | 53 | `DROP TABLE IF EXISTS gda_pre_sam_intel;` | DROP TABLE |
| 4 | `gda_target_agencies` | 5 | `DROP TABLE IF EXISTS gda_target_agencies;` | DROP TABLE |
| 5 | `gda_vehicle_tracker` | 14 | `DROP TABLE IF EXISTS gda_vehicle_tracker;` | DROP TABLE |

All 5 drops executed successfully. No FK ordering required (no inbound FKs).
Total rows removed: 111 (all stale orphan data with no consumers).

---

## Migration Files Generated

### 057_n8n_gda_relationships.sql

Creates `gda_relationships` table matching the live schema in `n8n-envision-postgres-1`.
This is the parent table for `gda_touchpoints` (migration 058).

- Path: `packages/backend/src/db/migrations/057_n8n_gda_relationships.sql`
- SHA-256: `ba69ed4a7b8f0393fa4d4654e5153a8059b87f92ee3efc0a902e4a720c135dd6`
- PII columns: `email`, `phone` (flagged in F-023 audit)
- Uses `CREATE TABLE IF NOT EXISTS` for idempotency

### 058_n8n_gda_touchpoints.sql

Creates `gda_touchpoints` table with FK to `gda_relationships`.
Companion table for the relationship-tracker feature.

- Path: `packages/backend/src/db/migrations/058_n8n_gda_touchpoints.sql`
- SHA-256: `93ad6c6a25e7840cfdd87e1a136787c64dc43e38dbbc554f267a6d34feafd6e6`
- FK: `relationship_id REFERENCES gda_relationships(id)`
- Depends on migration 057 (must run in order)

**Note:** These migrations are NOT run against production in this PR. CI smoke test validates
them. F-023c (execution of all ADOPT migrations) is a separate gated PR.

---

## Post-State Verification

### Table Count

| Metric | Before | After | Δ |
|--------|--------|-------|---|
| n8n DB total tables | 161 | 156 | -5 |
| Shadow tables | 69 | 64 | -5 |
| ADOPT | 27 | 28 | +1 |
| DEFERRED | 6 | 0 | -6 |
| DROPPED (cumulative) | 12 | 17 | +5 |

### Dropped Tables — 404 Confirmation

All 5 tables confirmed absent from `information_schema.tables`.

### gda_touchpoints — Still Present

`gda_touchpoints` confirmed still present in n8n-envision-postgres-1, 0 rows.
The workflow `GDA.api.relationship-tracker` still expects it. The migration prepares
the schema definition for future F-026 Step 3 consolidation, not an immediate move.

### ADOPT Integrity Spot-Check

| Table | Rows (before) | Rows (after) | Status |
|-------|-------------|-------------|--------|
| `gda_opportunity_tracker` | 1,780 | 1,780 | ✓ Unchanged |
| `gda_relationships` | 0 | 0 | ✓ Unchanged |
| `risk_register` | 459 | 459 | ✓ Unchanged |

### Canary Workflows

| Canary | ID | Last Success |
|--------|----|-------------|
| `GDA.cron.system-watchdog` | LPUSYd4Vpph1Qg7n | 2026-05-22 15:50 UTC |
| `GDA.cron.change-detector` | Zb2quk78c5mszZ2C | 2026-05-22 15:55 UTC |

### Endpoint Health

| Endpoint | Status |
|----------|--------|
| `gda.csr-llc.tech/health` | 200 |
| `n8n.csr-llc.tech/healthz` | 200 |
| `mcp.csr-llc.tech/mcp` | 200 |

---

## Classification Updates Applied

In `docs/audits/f023-shadow-schema-2026-05-22.md`:

1. 5 deferred tables marked DROPPED (added to DROPPED table, rows 13-17)
2. `gda_touchpoints` promoted from DEFERRED to ADOPT (#28 in ADOPT table)
3. DEFERRED section marked RESOLVED (0 remaining)
4. Bucket counts updated: ADOPT 28 / DOCUMENT-ONLY 36 / DROPPED 17 / DEFERRED 0
5. Shadow table total: 81 → 64
6. F-026 impact section updated: 2 of 28 ADOPT tables now have migrations
7. Recommended actions updated for ADOPT bucket migration status
